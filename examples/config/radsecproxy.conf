# Example placeholder only; this is not a runnable RadSec configuration.
# Put your peers, routing, and TLS settings in config/radsecproxy.conf.
# Keep certificates and private keys inside the ignored config/ folder.
